{
  "facts": {
    "environment not found": "Environment does not exist.",
    "package conflict": "Package installation fails due to dependency conflicts.",
    "conda not in path": "Conda command not found in PATH.",
    "conda not installed": "",
    "unable to call conda": "Conda might not in PATH or user have not run ```conda init```"
  },
  "rules": [
    {
      "conditions": [{"attribute": "env_not_found", "value": true}],
      "conclusion": "Run 'conda env list' to check environments or recreate with 'conda create --name <env>'."
    },
    {
      "conditions": [{"attribute": "conda_not_in_path", "value": true}],
      "conclusion": "Add Conda to PATH or run 'conda init bash' and restart shell."
		}
  ]
}
